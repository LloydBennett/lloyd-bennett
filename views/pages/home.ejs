<section class="section--l">
  <div class="container">
    <% 
    let title = document.data.title
    let titleArr = title.split(" ")
    
    %>
    <h1 class="type--xl">
      <% titleArr.forEach((text, index) => { %>
        <% let i = index + 1  %>

        <% if (i === titleArr.length) { %>
          <span class="type--italic"><%- text %></span>
        <% } else { %>
          <span><%- text %></span>
        <% } %>
      <% }) %>

    </h1>
  </div>


</section>
